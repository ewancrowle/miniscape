package net.miniscape.util.log.command;

import net.miniscape.command.Command;
import net.miniscape.player.Rank;

@Command(names = {"loglevel"}, rank = Rank.DEV)
public class LogLevelCommand {
    // change log level
    // required rank dev
}
